#!/usr/bin/env python

from enum import Enum

class CarActions(Enum):
    """
    Defines the possible actions that a car can take.
    """
    UP = "up"
    DOWN = "down"
    LEFT = "left"
    RIGHT = "right"

